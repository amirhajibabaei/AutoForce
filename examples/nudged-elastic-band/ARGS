calculator = 'EMT'

# ML specific (customize)
ediff = 0.2*kcal_mol
fdiff = 0.2*kcal_mol

# NEB specific
#rel_if = 0         # do not relax initial and final images, since already relaxed
rel_if = 1          # relax initial and final images before starting NEB
#rel_if = 2         # relax initial and final images every time NEB restarts
fmax = 0.01         # maximum residual force
climb = True        # climbing image algorithm
